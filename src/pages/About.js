import styled from "styled-components";

const About = () => {
  return <Wrapper className="main-page"></Wrapper>;
};

const Wrapper = styled.div`
  background: #222;
  width: 80vw;
  z-index: 1;
  position: relative;
`;

export default About;
